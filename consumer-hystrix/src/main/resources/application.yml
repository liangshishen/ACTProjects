# server
server:
  port: 9001

# spring
spring:
  application:
    name: consumer-hystrix

# eureka
eureka:
  client:
    service-url:
      defaultZone: http://user:password123@localhost:8761/eureka

# logging
logging:
  level:
    root: info