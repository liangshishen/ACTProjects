# server
server:
  port: 8020

# spring
spring:
  application:
    name: consumer-feign

# eureka
eureka:
  client:
    service-url:
      defaultZone: http://user:password123@localhost:8761/eureka

# logging
logging:
  level:
    root: info